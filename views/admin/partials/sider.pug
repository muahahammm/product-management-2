.sider
    .inner-menu
        ul.menu-list
            li 
                a.menu-link(href=`${prefixAdmin}/dashboard`) Tổng quan
            if(role.permissions.includes("product-category_view"))
                li 
                    a.menu-link(href=`${prefixAdmin}/products-category`) Danh mục sản phẩm
            if(role.permissions.includes("product_view"))
                li 
                    a.menu-link(href=`${prefixAdmin}/products`) Danh sách sản phẩm
            if(role.permissions.includes("roles_view"))
                li 
                    a.menu-link(href=`${prefixAdmin}/roles`) Nhóm quyền
            if(role.permissions.includes("roles"))
                li 
                    a.menu-link(href=`${prefixAdmin}/roles/permissions`) Phân quyền
            if(role.permissions.includes("accounts_view"))
                li 
                    a.menu-link(href=`${prefixAdmin}/accounts`) Danh sách tài khoản
            li 
                a.menu-link(href=`${prefixAdmin}/articles-category`) Danh mục bài viết
            li 
                a.menu-link(href=`${prefixAdmin}/articles`) Danh sách bài viết

    